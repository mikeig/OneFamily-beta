<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chaohan.onefamily">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/OnefamilyTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/OnefamilyStartTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainPageActivity"
            android:label="@string/title_activity_main2"
            android:theme="@style/OnefamilyTheme" />
        <activity
            android:name=".SignupActivity"
            android:parentActivityName=".MainActivity"
            android:theme="@style/OnefamilyTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:parentActivityName=".MainActivity"
            android:theme="@style/OnefamilyTheme" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".ResetPassActivity"
            android:parentActivityName=".LoginActivity"
            android:theme="@style/OnefamilyTheme" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".ProfileActivity"
            android:parentActivityName=".MainPageActivity"
            android:theme="@style/OnefamilyTheme" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".PostActivity"
            android:parentActivityName=".MainPageActivity"/>
        <activity android:name=".CheckActivity"
            android:parentActivityName=".MainPageActivity"/>
        <activity
            android:name=".SettingActivity"
            android:parentActivityName=".ProfileActivity"
            android:theme="@style/OnefamilyTheme" />
        <activity android:name=".FriendsActivity"
            android:parentActivityName=".MainPageActivity"/>
        <activity android:name=".TaskAndEventActivity"
            android:parentActivityName=".CheckActivity"/>
        <activity
            android:name=".NeighborActivity"
            android:label="@string/title_activity_neighbor"
            android:theme="@style/OnefamilyTheme.NoActionBar" />
        <activity android:name=".ObjectDetailActivity"
            android:parentActivityName=".TaskAndEventActivity"/>
        <activity android:name=".CollectionsActivity"
            android:parentActivityName=".ProfileActivity"/>
        <activity android:name=".MessagesActivity"
            android:parentActivityName=".ProfileActivity"/>
        <activity android:name=".RewardsActivity"
            android:parentActivityName=".ProfileActivity"/>
        <activity android:name=".InvitationActivity" />
        <activity android:name=".PrivacySetActivity"
            android:parentActivityName=".SettingActivity"/>
        <activity android:name=".FriendDetailActivity"
            android:parentActivityName=".FriendsActivity"/>
        <activity android:name=".FeedsActivity"
            android:parentActivityName=".MainPageActivity"></activity>
    </application>

</manifest>